# Optional: a FRED API key enables exact series metadata (titles/units) and a
# higher rate limit when fetching data at build time. Without it the build
# falls back to the public keyless FRED endpoint (throttled).
# Get a free key: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=
